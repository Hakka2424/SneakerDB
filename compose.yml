version: '3.4'

services:
    
  node:
    restart: always
    container_name: mongo_test
    environment:
      NODE_NUMBER: 1
    ports:
      - '15880:3000' #Port number changed
    build:
      context: .
      dockerfile: ./Dockerfile.node
    depends_on:
      mongo:
        condition: service_healthy
        restart: true

  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    volumes: 
      database_volume:/data/db

volumes:
  database_volume:


    

